{
  "permissions": {
    "allow": [
      "Bash(git commit -m 'Add demo data seeder for screenshots *)",
      "Bash(git commit -m ' *)",
      "PowerShell(Get-ChildItem -Path src -Recurse -Directory -Include bin,obj | ForEach-Object { Remove-Item -Recurse -Force -LiteralPath $_.FullName -ErrorAction SilentlyContinue }; Write-Output \"bin/obj cleanup done\")",
      "PowerShell(Get-ChildItem -Path src -Recurse -Directory -Include bin,obj | ForEach-Object { Remove-Item -Recurse -Force -LiteralPath $_.FullName -ErrorAction SilentlyContinue }; Remove-Item -Recurse -Force 'publish/customer-deploy' -ErrorAction SilentlyContinue; New-Item -ItemType Directory -Path 'publish/customer-deploy' -Force | Out-Null; Write-Output 'cleaned, ready for sequential publish')",
      "PowerShell(Get-ChildItem 'publish/customer-deploy' -Directory | ForEach-Object { $folder = $_.FullName; $name = $_.Name; $sizeMB = [math]::Round\\(\\(Get-ChildItem $folder -Recurse -File | Measure-Object -Property Length -Sum\\).Sum / 1MB, 1\\); $fileCount = \\(Get-ChildItem $folder -Recurse -File\\).Count; $exeName = switch \\($name\\) { 'main-app' { 'AccessControlPro.WPF.exe' }; 'admin-panel' { 'AccessControlPro.Admin.exe' }; 'pos-terminal' { 'AccessControlPro.POS.exe' } }; $exePath = Join-Path $folder $exeName; $exeOk = Test-Path $exePath; Write-Output \\(\"{0,-15} size={1,6} MB  files={2,4}  {3} {4}\" -f $name, $sizeMB, $fileCount, $exeName, $\\(if \\($exeOk\\) { 'OK' } else { 'MISSING' }\\)\\) })",
      "PowerShell(New-Item -ItemType Directory -Path 'src/AccessControlPro.Web/wwwroot/images/screenshots' -Force)",
      "PowerShell(Get-ChildItem -Path src -Recurse -Directory -Include bin,obj | ForEach-Object { Remove-Item -Recurse -Force -LiteralPath $_.FullName -ErrorAction SilentlyContinue }; Remove-Item -Recurse -Force 'publish/customer-deploy' -ErrorAction SilentlyContinue; Write-Output 'cleaned')",
      "PowerShell(Get-Process | Where-Object { $_.ProcessName -match 'AccessControlPro' } | Select-Object Id, ProcessName, Path | Format-Table -AutoSize)",
      "PowerShell(Get-ChildItem 'publish/customer-deploy' -Directory | ForEach-Object { $folder = $_.FullName; $name = $_.Name; $sizeMB = [math]::Round\\(\\(Get-ChildItem $folder -Recurse -File | Measure-Object -Property Length -Sum\\).Sum / 1MB, 1\\); $exeName = switch \\($name\\) { 'main-app' { 'AccessControlPro.WPF.exe' }; 'admin-panel' { 'AccessControlPro.Admin.exe' }; 'pos-terminal' { 'AccessControlPro.POS.exe' } }; $exePath = Join-Path $folder $exeName; $exeOk = Test-Path $exePath; Write-Output \\(\"{0,-15} {1,6} MB  {2} {3}\" -f $name, $sizeMB, $exeName, $\\(if \\($exeOk\\) { 'OK' } else { 'MISSING' }\\)\\) })",
      "PowerShell($running = Get-Process | Where-Object { $_.ProcessName -match 'AccessControlPro' }; if \\($running\\) { Write-Output \"STOP: AccessControlPro processes still running:\"; $running | Select-Object Id, ProcessName | Format-Table -AutoSize } else { Write-Output 'no running processes' })",
      "PowerShell(Get-ChildItem -Path src -Recurse -Directory -Include bin,obj -ErrorAction SilentlyContinue | ForEach-Object { Remove-Item -Recurse -Force -LiteralPath $_.FullName -ErrorAction SilentlyContinue }; Remove-Item -Recurse -Force 'publish/customer-deploy' -ErrorAction SilentlyContinue; New-Item -ItemType Directory -Path 'publish/customer-deploy' -Force | Out-Null; Write-Output 'cleaned')",
      "PowerShell(Get-ChildItem 'publish/customer-deploy' -Directory | ForEach-Object { $folder = $_.FullName; $name = $_.Name; $sizeMB = [math]::Round\\(\\(Get-ChildItem $folder -Recurse -File | Measure-Object -Property Length -Sum\\).Sum / 1MB, 1\\); $fileCount = \\(Get-ChildItem $folder -Recurse -File\\).Count; $exeName = switch \\($name\\) { 'main-app' { 'AccessControlPro.WPF.exe' }; 'admin-panel' { 'AccessControlPro.Admin.exe' }; 'pos-terminal' { 'AccessControlPro.POS.exe' } }; $exePath = Join-Path $folder $exeName; $exeOk = Test-Path $exePath; $exeBuild = if \\($exeOk\\) { \\(Get-Item $exePath\\).LastWriteTime.ToString\\('yyyy-MM-dd HH:mm'\\) } else { 'n/a' }; Write-Output \\(\"{0,-15} {1,6} MB  files={2,4}  built={3}  {4} {5}\" -f $name, $sizeMB, $fileCount, $exeBuild, $exeName, $\\(if \\($exeOk\\) { 'OK' } else { 'MISSING' }\\)\\) })",
      "PowerShell($running = Get-Process | Where-Object { $_.ProcessName -match 'AccessControlPro' }; if \\($running\\) { Write-Output 'STOP: still running'; $running | Format-Table } else { Get-ChildItem -Path src -Recurse -Directory -Include bin,obj -ErrorAction SilentlyContinue | ForEach-Object { Remove-Item -Recurse -Force -LiteralPath $_.FullName -ErrorAction SilentlyContinue }; Remove-Item -Recurse -Force 'publish/customer-deploy' -ErrorAction SilentlyContinue; New-Item -ItemType Directory -Path 'publish/customer-deploy' -Force | Out-Null; Write-Output 'cleaned' })",
      "PowerShell(Get-ChildItem 'publish/customer-deploy' -Directory | ForEach-Object { $folder = $_.FullName; $name = $_.Name; $sizeMB = [math]::Round\\(\\(Get-ChildItem $folder -Recurse -File | Measure-Object -Property Length -Sum\\).Sum / 1MB, 1\\); $exeName = switch \\($name\\) { 'main-app' { 'AccessControlPro.WPF.exe' }; 'admin-panel' { 'AccessControlPro.Admin.exe' }; 'pos-terminal' { 'AccessControlPro.POS.exe' } }; $exePath = Join-Path $folder $exeName; $exeOk = Test-Path $exePath; $exeBuild = if \\($exeOk\\) { \\(Get-Item $exePath\\).LastWriteTime.ToString\\('yyyy-MM-dd HH:mm'\\) } else { 'n/a' }; Write-Output \\(\"{0,-15} {1,6} MB  built={2}  {3} {4}\" -f $name, $sizeMB, $exeBuild, $exeName, $\\(if \\($exeOk\\) { 'OK' } else { 'MISSING' }\\)\\) })"
    ],
    "additionalDirectories": [
      "d:\\AccessControlPro\\tools"
    ]
  }
}
