<UserControl x:Class="AccessControlPro.Admin.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:helpers="clr-namespace:AccessControlPro.WPF.Helpers;assembly=AccessControlPro.WPF"
             Background="{StaticResource BgDarkBrush}">

    <UserControl.Resources>
        <helpers:BindingProxy x:Key="Proxy" Data="{Binding}"/>
    </UserControl.Resources>

    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="{Binding Lang.PrdTitle}" FontSize="24"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock Text="{Binding Lang.PrdSubtitle}" FontSize="12"
                           Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
            </StackPanel>
        </DockPanel>

        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Products DataGrid -->
            <Border Grid.Column="0" Style="{StaticResource DashboardCard}" Margin="0,0,8,0">
              <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Search bar -->
                <Border Grid.Row="0" Background="{StaticResource BgInputBrush}" CornerRadius="8"
                        Padding="10,0" Margin="0,0,0,12">
                    <DockPanel>
                        <Button DockPanel.Dock="Right" Style="{StaticResource SecondaryButton}" Padding="14,6"
                                Margin="8,4" Command="{Binding StockTakeCommand}" Cursor="Hand">
                            <StackPanel Orientation="Horizontal">
                                <fa:ImageAwesome Icon="Calculator" Width="13" Height="13"
                                                 VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="{Binding Lang.PrdStockTake}" FontSize="12" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <fa:ImageAwesome Icon="Search" Width="13" Height="13" DockPanel.Dock="Left"
                                         Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextPrimaryBrush}"
                                 CaretBrush="{StaticResource TextPrimaryBrush}" FontFamily="{StaticResource CairoFont}"
                                 FontSize="13" Padding="0,9" VerticalContentAlignment="Center"/>
                    </DockPanel>
                </Border>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Products}"
                          SelectedItem="{Binding SelectedProduct}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          Background="Transparent"
                          BorderThickness="0"
                          RowBackground="#0E2A2D"
                          AlternatingRowBackground="#112F32"
                          Foreground="{StaticResource TextPrimaryBrush}"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#15FFFFFF"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          CanUserResizeRows="False"
                          FontFamily="{StaticResource CairoFont}"
                          FontSize="13"
                          FontWeight="Bold">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#143436"/>
                            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                            <Setter Property="FontFamily" Value="{StaticResource CairoFont}"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="14,10"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridColumnHeader">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"
                                                              TextBlock.Foreground="{TemplateBinding Foreground}"
                                                              TextBlock.FontFamily="{TemplateBinding FontFamily}"
                                                              TextBlock.FontSize="{TemplateBinding FontSize}"
                                                              TextBlock.FontWeight="{TemplateBinding FontWeight}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="14,8"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{Binding Data.Lang.PrdName, Source={StaticResource Proxy}}"
                                            Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="{Binding Data.Lang.PrdNameAr, Source={StaticResource Proxy}}"
                                            Binding="{Binding NameAr}" Width="*"/>
                        <DataGridTextColumn Header="{Binding Data.Lang.PrdBarcode, Source={StaticResource Proxy}}"
                                            Binding="{Binding Barcode}" Width="120"/>
                        <DataGridTextColumn Header="{Binding Data.Lang.PrdPrice, Source={StaticResource Proxy}}"
                                            Binding="{Binding Price, StringFormat={}{0:N0}}" Width="90"/>
                        <DataGridTextColumn Header="{Binding Data.Lang.PrdCostPrice, Source={StaticResource Proxy}}"
                                            Binding="{Binding CostPrice, StringFormat={}{0:N0}}" Width="90"/>
                        <DataGridTextColumn Header="{Binding Data.Lang.PrdProfit, Source={StaticResource Proxy}}"
                                            Binding="{Binding Profit, StringFormat={}{0:N0}}" Width="90"/>
                        <DataGridTextColumn Header="{Binding Data.Lang.PrdCategory, Source={StaticResource Proxy}}"
                                            Binding="{Binding Category}" Width="110"/>
                        <DataGridTextColumn Header="{Binding Data.Lang.PrdStock, Source={StaticResource Proxy}}"
                                            Binding="{Binding Stock}" Width="70"/>
                    </DataGrid.Columns>
                </DataGrid>
              </Grid>
            </Border>

            <!-- Right: Add/Edit Panel -->
            <Border Grid.Column="1" Style="{StaticResource DashboardCard}" Margin="8,0,0,0">
                <StackPanel>
                    <!-- Panel Title -->
                    <DockPanel Margin="0,0,0,20">
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                            <fa:ImageAwesome Icon="PlusCircle" Width="16" Height="16"
                                             Foreground="{StaticResource PrimaryLightBrush}"
                                             VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock FontSize="16" Foreground="{StaticResource TextPrimaryBrush}"
                                       VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding Lang.PrdAddNew}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                <Setter Property="Text" Value="{Binding Lang.PrdEdit}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                        <Button DockPanel.Dock="Right" Background="Transparent" BorderThickness="0"
                                Cursor="Hand" Command="{Binding CancelEditCommand}"
                                HorizontalAlignment="Right"
                                Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibility}}">
                            <fa:ImageAwesome Icon="Times" Width="14" Height="14"
                                             Foreground="{StaticResource TextMutedBrush}"/>
                        </Button>
                    </DockPanel>

                    <!-- Name (EN) -->
                    <TextBlock Text="{Binding Lang.PrdName}" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource DarkTextBox}" Margin="0,0,0,14"/>

                    <!-- Name (AR) -->
                    <TextBlock Text="{Binding Lang.PrdNameAr}" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding EditNameAr, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource DarkTextBox}" Margin="0,0,0,14"
                             FlowDirection="RightToLeft"/>

                    <!-- Barcode -->
                    <TextBlock Text="{Binding Lang.PrdBarcode}" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding EditBarcode, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource DarkTextBox}" Margin="0,0,0,14"/>

                    <!-- Price (sell) -->
                    <TextBlock Text="{Binding Lang.PrdPrice}" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding EditPrice, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource DarkTextBox}" Margin="0,0,0,14"/>

                    <!-- Cost Price (buy) — usually filled automatically from purchase orders -->
                    <TextBlock Text="{Binding Lang.PrdCostPrice}" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding EditCostPrice, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource DarkTextBox}" Margin="0,0,0,14"/>

                    <!-- Category -->
                    <TextBlock Text="{Binding Lang.PrdCategory}" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <ComboBox ItemsSource="{Binding Categories}"
                              SelectedItem="{Binding EditCategory, Mode=TwoWay}"
                              Style="{StaticResource DarkComboBox}" Margin="0,0,0,14"/>

                    <!-- Reorder level — low-stock alert threshold (0 = no alert) -->
                    <TextBlock Text="{Binding Lang.PrdReorderLevel}" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding EditReorderLevel, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource DarkTextBox}" Margin="0,0,0,14"/>

                    <!-- Expiry date — optional; blocks selling past it + drives expiry alerts -->
                    <TextBlock Text="{Binding Lang.PrdExpiry}" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <DatePicker SelectedDate="{Binding EditExpiryDate, Mode=TwoWay}" Margin="0,0,0,14"/>

                    <Border Height="1" Background="#15FFFFFF" Margin="0,4,0,14"/>

                    <!-- Buttons -->
                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource PrimaryButton}" Padding="20,10"
                                Command="{Binding SaveCommand}" Margin="0,0,10,0" Cursor="Hand">
                            <StackPanel Orientation="Horizontal">
                                <fa:ImageAwesome Icon="Check" Width="12" Height="12"
                                                 Foreground="White" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="{Binding Lang.UsrSave}" FontSize="13" Foreground="White" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource SecondaryButton}" Padding="20,10"
                                Command="{Binding DeleteCommand}" Cursor="Hand"
                                Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibility}}">
                            <StackPanel Orientation="Horizontal">
                                <fa:ImageAwesome Icon="Trash" Width="12" Height="12"
                                                 Foreground="{StaticResource DangerBrush}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="{Binding Lang.CatDelete}" FontSize="13"
                                           Foreground="{StaticResource DangerBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
