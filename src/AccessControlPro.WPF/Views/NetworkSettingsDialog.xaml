<Window x:Class="AccessControlPro.WPF.Views.NetworkSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Network Settings" Width="400" Height="280"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="#1E1E2E">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Network Settings"
                   FontSize="18" FontWeight="SemiBold" Foreground="#E0E0E0"
                   Margin="0,0,0,16"/>

        <!-- IP Address -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="IP Address:"
                   Foreground="#B0B0B0" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox x:Name="IpTextBox" Grid.Row="1" Grid.Column="1"
                 FontSize="14" Padding="8,6" Margin="0,0,0,10"
                 Background="#2A2A3E" Foreground="#E0E0E0" BorderBrush="#444"
                 CaretBrush="#E0E0E0"/>

        <!-- Subnet Mask -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Subnet Mask:"
                   Foreground="#B0B0B0" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox x:Name="SubnetTextBox" Grid.Row="2" Grid.Column="1"
                 FontSize="14" Padding="8,6" Margin="0,0,0,10"
                 Background="#2A2A3E" Foreground="#E0E0E0" BorderBrush="#444"
                 CaretBrush="#E0E0E0"/>

        <!-- Gateway -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Gateway:"
                   Foreground="#B0B0B0" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox x:Name="GatewayTextBox" Grid.Row="3" Grid.Column="1"
                 FontSize="14" Padding="8,6" Margin="0,0,0,10"
                 Background="#2A2A3E" Foreground="#E0E0E0" BorderBrush="#444"
                 CaretBrush="#E0E0E0"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="CancelButton" Content="Cancel" Width="80" Height="32"
                    Margin="0,0,10,0" Click="CancelButton_Click"
                    Background="#444" Foreground="#E0E0E0" BorderBrush="#555"
                    Cursor="Hand"/>
            <Button x:Name="OkButton" Content="OK" Width="80" Height="32"
                    Click="OkButton_Click" IsDefault="True"
                    Background="#44A1A0" Foreground="White" BorderBrush="#44A1A0"
                    FontWeight="SemiBold" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
